{
  "name": "blaze-analytics-collector",
  "version": "1.3.0",
  "private": true,
  "description": "Teste de coleta direta independente no Railway EU West",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/collector.js && node --check src/database.js && node --check src/calculations.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pg": "^8.16.0"
  }
}
