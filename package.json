{
  "name": "blaze-analytics-collector",
  "version": "1.1.0",
  "private": true,
  "description": "API e banco do Blaze Analytics com ingestão enviada pela extensão",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/collector.js && node --check src/database.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pg": "^8.16.0"
  }
}
